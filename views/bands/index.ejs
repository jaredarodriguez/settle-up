<%- include('../partials/header') %>

<table id="bandRoster">
  <thead>
    <th>Band Roster</th>
  </thead>
  <tbody>
    <% bands.forEach(function(b) { %>
    <tr>
      <td><%= b.title %></td>
      <td><%= b.memberName %></td>
    </tr>
    <% }); %>
  </tbody>
</table>

<%- include('../partials/footer') %>
